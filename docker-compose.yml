services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - db-data:/data
    restart: unless-stopped
    # Graceful stop lets the server flush all rooms to SQLite.
    stop_grace_period: 15s

volumes:
  db-data:
